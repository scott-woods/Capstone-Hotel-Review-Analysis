The site can be accessed at https://hotel-rating-estimator.herokuapp.com/ (NOTE: It may take up to 30 seconds to load. 
If it comes up with a Heroku error, refreshing the page should load it properly.)

The Username and Password to login are both "admin"
